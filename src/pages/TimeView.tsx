import { Fragment } from "react";
import {
    Clock,
} from "../components";
import React from "react";

export const TimeView = () => (
    <div>
        <div className='clock-content'>
            <Clock />
        </div>
    </div>
);
